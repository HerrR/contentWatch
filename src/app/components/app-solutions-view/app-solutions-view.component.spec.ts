/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AppSolutionsViewComponent } from './app-solutions-view.component';

describe('Component: AppSolutionsView', () => {
  it('should create an instance', () => {
    let component = new AppSolutionsViewComponent();
    expect(component).toBeTruthy();
  });
});
